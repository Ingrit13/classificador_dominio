# Preferível: instalar o projeto em modo editável (lê dependências do pyproject.toml)
-e .

# Extras opcionais (mesmos grupos do pyproject.toml):
#   pip install -e ".[api]"
#   pip install -e ".[jupyter]"
#   pip install -e ".[dev]"
